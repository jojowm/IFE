window.onload = function () {
    setHeight();
}

function setHeight () {
    var oSidebar = document.getElementById('sidebar-outer');
    var totalHeight = document.body.clientHeight || document.documentElement.clientHeight;
    console.log(document.body.clientHeight, document.documentElement.clientHeight, window.innerHeight)
    // console.log()
    // oSidebar.style.height =
}